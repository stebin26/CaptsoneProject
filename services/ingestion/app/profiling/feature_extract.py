"""Feature extraction helpers for the profiling stage.

Holds the derivations that turn a raw column profile into the signals the
mapping suggester scores against, kept separate from the profiler so the
evidence-gathering and the interpretation of that evidence stay distinct.
"""
