"""Reusable signal shapes for the data generators.

Holds the seasonality, trend, and noise patterns the industry builders combine,
kept separate so the same shapes stay consistent across every generated
dataset.
"""
